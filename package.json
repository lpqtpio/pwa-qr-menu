{
  "name": "pwa-qr-menu",
  "private": true,
  "type": "module",
  "scripts": {
    "vercel-build": "cd frontend && npm ci && npm run build && mkdir -p ../public && cp -r dist/* ../public/",
    "build:frontend": "cd frontend && npm ci && npm run build",
    "build:backend": "cd backend && npm ci",
    "build": "npm run build:frontend"
  },
  "dependencies": {
    "dotenv": "^17.3.1",
    "jsonwebtoken": "^9.0.3",
    "mongoose": "^9.3.0"
  }
}
